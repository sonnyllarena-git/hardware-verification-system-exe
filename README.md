# frontend-hardware-check-exe
Windows standalone application (C# .NET). Collects applicant PC specs: OS version, CPU cores, RAM, storage type/size, screen resolution, internet speed, webcam/headset detection, applicant name/email. Submits data to backend API via unique API key. ~50-100KB signed binary for distribution via HR.
